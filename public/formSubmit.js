// Handle short form and coreg answer submission

function submitForm(data) {
    if (data.type === 'short') {
        handleShortForm(data);
    } else {
        handleCoregAnswer(data);
    }
}

function handleShortForm(data) {
    // Implementation for handling short form submission
}

function handleCoregAnswer(data) {
    // Implementation for handling coreg answer submission
}
